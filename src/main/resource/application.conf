outlier {
   typ = "ThreshOutlier"
   directory="C://Users//wangc//Results//WhereIsOutlierScala//bio_sensor"
   fileName="patient14"
   R = 5
   k = 6
}
win {
   width = 12.0
   slideLen = 3
}
dataset {
   directory="C://Users//wangc//DataSet//CleanedData"
   middle="//Patient14.csv//"
   dataFile="part-00000"
   
}
dataattr {
   name="year,month,day,hour,avg(Z-axis),avg(Y-axis),avg(X-axis),avg(Celsius),avg(EDA),avg(Battery),avg(Event)"
   label="None"
   type="(year,nominal,Int) (month,nominal,Int) (day,nominal,Int) (hour,nominal,Int) (minute,nominal,Int) (second,nominal,Int) (Z_axis,numeric,Double) (Y_axis,numeric,Double) (X_axis,numeric,Double) (Celsius,numeric,Double) (EDA,numeric,Double) (avg(Battery),numeric,Double) (avg(Event),numeric,Double)"
   notUsed="year,month,day,hour"   
   used="Z_axis,Y_axis,X_axis,Celsius,EDA"
}
